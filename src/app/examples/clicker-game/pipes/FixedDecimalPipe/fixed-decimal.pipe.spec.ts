import { FixedDecimalPipePipe } from './fixed-decimal.pipe';

describe('FixedDecimalPipePipe', () => {
  it('create an instance', () => {
    const pipe = new FixedDecimalPipePipe();
    expect(pipe).toBeTruthy();
  });
});
